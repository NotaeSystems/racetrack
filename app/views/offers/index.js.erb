<%if @race.betting_status == 'Open' %>
  $('#buy_offers').html("<%= escape_javascript(render(:partial => 'buy_offers'))%>");
  $('#sell_offers').html("<%= escape_javascript(render(:partial => 'sell_offers'))%>");
  $('#open_contracts').html("<%= escape_javascript(render(:partial => 'open_contracts'))%>");
<%else%>
  $('#offers').html("<%= escape_javascript(@race.status)%>");
<%end%>
