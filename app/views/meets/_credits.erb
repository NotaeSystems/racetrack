<% if user_signed_in? %>
  <h4>Credit Balance for <%=@meet.name%>: <%=current_user.meet_balance(@meet) %></h4>
  <%if current_user.meet_balance(@meet) == 0 %>
    <%= link_to "Refresh Credits", refresh_credits_meet_path(:id => @meet.id) %>
  <%end%>
<%else%>
<h3>Login or Signup for Free 100 Credits</h3>
<%end%>
<%= link_to "Rankings", rankings_path(:meet_id => @meet.id), :class => 'btn btn-mini'%>
