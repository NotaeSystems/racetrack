<h1> <%= link_to @league.name, @league %></h1>

<h1>Members</h1>

<table class='table table-striped table-bordered table-condensed'>
  <tr>
    <th>Member</th>
    <th>League</th>
    <th>Status</th>
    <th>Nickname</th>
    <th>Active</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @leagueusers.each do |leagueuser| %>
  <tr>
    <td><%= leagueuser.user_id %></td>
    <td><%= leagueuser.league_id %></td>
    <td><%= leagueuser.status %></td>
    <td><%= leagueuser.nickname %></td>
    <td><%= leagueuser.active %></td>
    <td><%= link_to 'Show', leagueuser %></td>
    <td><%= link_to 'Edit', edit_leagueuser_path(leagueuser) %></td>
    <td><%= link_to 'Destroy', leagueuser, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Leagueuser', new_leagueuser_path %>
